name: Echo Workflow

on:
  push:
    branches:
      - main

jobs:
  echo_job:
    runs-on: prod
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Echo a message
        run: echo "Hello from GitHub Actions!"
